/* eslint-disable no-undef */
const {
   mayuscula,
   invocarCallback,
   operacionMatematica,
   sumarArray,
   forEach,
   map,
   filter,
} = require('../homework');

describe('JAVASCRIPT VI', function () {

});
